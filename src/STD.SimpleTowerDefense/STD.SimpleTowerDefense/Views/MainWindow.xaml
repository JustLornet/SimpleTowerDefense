<Window x:Class="STD.SimpleTowerDefense.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STD.SimpleTowerDefense.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="{Binding TestTitle}" Height="1000" Width="1000">
    <Grid>
        <ContentControl prism:RegionManager.RegionName="Map" />
        <!--<Button Height="30" Width="30" VerticalAlignment="Top" Command="{Binding TestCommand}">add test</Button>
        <ContentControl prism:RegionManager.RegionName="TestContentRegion" />-->
    </Grid>
</Window>
